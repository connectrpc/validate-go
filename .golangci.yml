version: "2"
linters:
  default: all
  disable:
    - cyclop            # covered by gocyclo
    - depguard          # unnecessary for small libraries
    - exhaustruct       # deprecated, replaced by exhaustruct_v5
    - funlen            # rely on code review to limit function length
    - gocognit          # dubious "cognitive overhead" quantification
    - goconst           # too many false positives
    - gomodguard        # deprecated, replaced by gomodguard_v2
    - inamedparam       # convention is not followed
    - ireturn           # "accept interfaces, return structs" isn't ironclad
    - lll               # don't want hard limits for line length
    - maintidx          # covered by gocyclo
    - nlreturn          # generous whitespace violates house style
    - noinlineerr       # inline is fine
    - nonamedreturns    # named returns are fine; it's *bare* returns that are bad
    - protogetter       # too many false positives
    - testpackage       # internal tests are fine
    - wrapcheck         # don't _always_ need to wrap errors
    - wsl               # deprecated, replaced by wsl_v5
    - wsl_v5            # generous whitespace violates house style
  settings:
    errcheck:
      check-type-assertions: true
    exhaustruct_v5:
      # Only check the types named below, rather than every struct literal.
      explicit-mode: true
      enforce-patterns:
        # No zero values for param structs.
        - 'connectrpc\.com/connect\..*[pP]arams'
    forbidigo:
      forbid:
        - pattern: '^fmt\.Print'
        - pattern: '^log\.'
        - pattern: '^print$'
        - pattern: '^println$'
        - pattern: '^panic$'
    godox:
      # TODO, OPT, etc. comments are fine to commit. Use FIXME comments for
      # temporary hacks, and use godox to prevent committing them.
      keywords: [ FIXME ]
    varnamelen:
      ignore-decls:
        - T any
        - i int
        - wg sync.WaitGroup
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      # Don't ban use of fmt.Errorf to create new errors, but the remaining
      # checks from err113 are useful.
      - linters: [ err113 ]
        text: "do not define dynamic errors, use wrapped static errors instead: .*"
formatters:
  enable:
    - gci               # rely on gci instead of goimports
    - gofmt             # prefer standard gofmt over gofumpt
